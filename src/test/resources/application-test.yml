spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:~/frameworkStudy
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show_sql: true
    properties:
      hibernate:
        format_sql: true